server:
  port: ${SUPPLIER_GATEWAY_PORT:8081}

spring:
  application:
    name: supplier-gateway

management:
  endpoints:
    web:
      exposure:
        include: health

logging:
  pattern:
    console: "%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%thread] [%X{traceId:-}] %logger{36} - %msg%n"
